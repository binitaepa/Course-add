import PropTypes from 'prop-types';

const Carts = props => {
    return (
        <div>
            <h1>this is cart section</h1>
        </div>
    );
};

Carts.propTypes = {
    
};

export default Carts;